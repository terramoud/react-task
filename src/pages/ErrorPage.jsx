export const ErrorPage = () => {
    return (
        <div>
            <h1 style={{color: "red"}}>
                You have entered a page that does not exist
            </h1>
        </div>
    )
}